spring:
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
  application:
    name: redis-demo-ab